app:
  title: SHIELD Platform
  baseUrl: http://localhost:3000

organization:
  name: SHIELD Platform

backend:
  baseUrl: http://localhost:7007
  listen:
    port: 7007
  csp:
    connect-src: ["'self'", 'http:', 'https:']
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  auth:
    keys:
      - secret: local-dev-secret
  database:
    client: better-sqlite3
    connection: ':memory:'
  reading:
    allow:
      - host: raw.githubusercontent.com
      - host: dev.azure.com

integrations:
  azure:
    - host: dev.azure.com
      # Export AZURE_DEVOPS_PAT before starting the backend to control Azure Repos access.
      token: ${AZURE_DEVOPS_PAT}

proxy:
  endpoints: {}

techdocs:
  builder: 'local'
  generator:
    runIn: 'docker'
  publisher:
    type: 'local'

auth:
  environment: development
  providers:
    guest:
      userEntityRef: user:default/danylo
      ownershipEntityRefs:
        - user:default/danylo
        - group:default/devops-team
        - group:default/shield-admins
      dangerouslyAllowOutsideDevelopment: true

scaffolder:
  defaultAuthor:
    name: ArgoIT Devops
    email: devopsacesso@useargo.com
  defaultCommitMessage: 'chore(scaffolder): bootstrap from SHIELD template'

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, Domain, API, Resource, Location, Template, User, Group]
  locations:
    - type: file
      target: ../../catalog-info.yaml
    - type: file
      target: ../../catalog/all.yaml
    - type: file
      target: ../../catalog/org.yaml
      rules:
        - allow: [User, Group]
    - type: file
      target: ../../templates/java-service/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../templates/dotnet-service/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../templates/python-service/template.yaml
      rules:
        - allow: [Template]

kubernetes:
  serviceLocatorMethod:
    type: multiTenant
  clusterLocatorMethods:
    - type: config
      clusters: []
  customResources:
    - group: argoproj.io
      apiVersion: v1alpha1
      plural: applications

permission:
  enabled: true

vault:
  # Produção: Hashicorp Vault com OIDC (Argo-DevOps)
  endpoint: ${VAULT_ADDR:https://tiopatinhas.argoit.net.br}
  authMethod: ${VAULT_AUTH_METHOD:oidc}
  
  # Configuração de OIDC (Argo-DevOps)
  oidc:
    role: ${VAULT_OIDC_ROLE:backstage}
    provider: ${VAULT_OIDC_PROVIDER:argo-devops}
    clientId: ${VAULT_OIDC_CLIENT_ID:}
  tokenFile: ${VAULT_TOKEN_FILE:~/.vault-token}
  
  # Configuração de Organização e Namespace
  namespace: ${VAULT_NAMESPACE:argo}
  organization: ${VAULT_ORGANIZATION:}
  
  # Token fallback para ambientes sem OIDC (desenvolvimento)
  # Após 'vault login -method=oidc', o backend também pode reutilizar ~/.vault-token
  # token: ${VAULT_TOKEN:}

shield:
  branding:
    productName: SHIELD Platform
    supportUrl: /docs/default/component/shield-platform
  integrations:
    azureDevOps:
      host: dev.azure.com
      organization: argosolutions
      defaultProject: Root Cause
      templatesRepo: poc-argo-code
      templatesBasePath: base-argoit
      gitOpsRepo: poc-argo-gitops
      catalogProvider: disabled
      branchPolicies:
        automaticReviewerPrincipalNames:
          - '[argosolutions]\Argo DevSecOps'
          - '[argosolutions]\Argo Infra'
    templating:
      syncRepository:
        enabled: true
        url: https://dev.azure.com/argosolutions/Devops/_git/argo-code
        branch: main
        localPath: .argo-code-sync
        pullRequestBranchName: backstage-template-sync
        templates:
          - java-service
          - dotnet-service
          - python-service
        autoSync: false
    argoCd:
      baseUrl: https://tower.argoit.net.br
      appNameAnnotation: shield.io/argocd-app-name
      projectAnnotation: shield.io/argocd-project
    terraform:
      stateBackends:
        - azurerm
