apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: backend-apps-manual-rc
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          - app: sample-api
            namespace: sample-team
  template:
    metadata:
      name: "{{app}}-rc"
      labels:
        shield.env: rc
        shield.cluster: sokovia
    spec:
      project: shield-backend
      source:
        repoURL: https://dev.azure.com/argosolutions/Root%20Cause/_git/poc-argo-gitops
        targetRevision: main
        path: "gitops/apps/backend/{{app}}/rc"
      destination:
        server: https://sokovia.cluster.local
        namespace: "{{namespace}}"
      syncPolicy:
        syncOptions:
          - CreateNamespace=true